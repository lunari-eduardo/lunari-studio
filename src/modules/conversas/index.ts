/**
 * Entry-point do módulo Conversas.
 *
 * Importado via side-effect em App.tsx (como os outros módulos).
 * Registra capabilities, eventos e singleton channels aqui.
 *
 * @module conversas
 */

// Re-exporta todos os tipos públicos
export * from './types';

// Capacidades registradas no registry global
// (serão adicionadas conforme as fases avançam)
